const style = (theme) => ({
    text: {
        fontFamily: theme.font,
        fontSize: 30,
        color: theme.green.dark,
        paddingTop: 20
    },
    text2: {
        fontFamily: theme.font,
        fontSize: 30,
        color: theme.orange.darkest,
        paddingTop: 10
    }
})
export default style;